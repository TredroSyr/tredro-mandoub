<?xml version="1.0" encoding="utf-8" ?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application
    android:allowBackup="true"
    android:icon="@mipmap/ic_launcher"
    android:label="@string/app_name"
    android:roundIcon="@mipmap/ic_launcher_round"
    android:supportsRtl="true"
    android:theme="@style/AppTheme"
  >
        <activity
      android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation|density"
      android:name=".MainActivity"
      android:label="@string/title_activity_main"
      android:theme="@style/AppTheme.NoActionBarLaunch"
      android:launchMode="singleTask"
      android:exported="true"
    >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="https" android:host="mandoub.tredro.online" />
                <!-- List app routes explicitly instead of claiming the whole host: intent
                     filters can't exclude paths, and an unrestricted filter makes
                     https://mandoub.tredro.online/download/tredro-mandoub.apk open back
                     inside the app (so the update button downloads nothing) rather than
                     in the browser. Add a pathPrefix here when adding a top-level route. -->
                <data android:path="/" />
                <data android:pathPrefix="/home" />
                <data android:pathPrefix="/map" />
                <data android:pathPrefix="/my-orders" />
                <data android:pathPrefix="/notifications" />
                <data android:pathPrefix="/orders" />
                <data android:pathPrefix="/sales" />
                <data android:pathPrefix="/settings" />
                <data android:pathPrefix="/stores" />
                <data android:pathPrefix="/sync-issues" />
                <data android:pathPrefix="/offline-debug" />
                <data android:pathPrefix="/auth" />
            </intent-filter>
        </activity>

        <!-- Without this, Android 8+ files backgrounded/killed FCM notifications under a
             low-importance fallback channel instead of the one @capacitor/push-notifications
             creates on this id (backend notifications.md §6). -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_channel_id"
            android:value="@string/default_notification_channel_id" />

        <!-- Status bar icon for FCM-displayed (backgrounded/killed) notifications. Android
             resolves drawable-night/ic_stat_notify.png automatically when the device is in
             system dark mode, and drawable/ic_stat_notify.png otherwise. -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_icon"
            android:resource="@drawable/ic_stat_notify" />

        <provider
      android:name="androidx.core.content.FileProvider"
      android:authorities="${applicationId}.fileprovider"
      android:exported="false"
      android:grantUriPermissions="true"
    >
            <meta-data
        android:name="android.support.FILE_PROVIDER_PATHS"
        android:resource="@xml/file_paths"
      />
        </provider>
    </application>

    <!-- Permissions -->

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
</manifest>
